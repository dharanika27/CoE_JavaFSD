import { HighlightChampionPipe } from './highlight-champion.pipe';

describe('HighlightChampionPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightChampionPipe();
    expect(pipe).toBeTruthy();
  });
});
